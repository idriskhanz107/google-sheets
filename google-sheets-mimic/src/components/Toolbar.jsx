import React from 'react';

const Toolbar = () => {
    return (
        <div className="toolbar">
            <button>B</button>
            <button>I</button>
            <button>U</button>
            {/* Add more formatting options */}
        </div>
    );
};

export default Toolbar;
